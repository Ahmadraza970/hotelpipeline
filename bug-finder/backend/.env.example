# LLM provider (OpenAI-compatible). Omit to use lint-only mode.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini

# CORS for the frontend dev server.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Where project/scan state is persisted.
BUGFINDER_DATA_DIR=./data/projects
